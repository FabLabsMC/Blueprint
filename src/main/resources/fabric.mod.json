{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${id}",
  "icon": "assets/blueprint/icon.png",
  "description": "Template for starting out FabLabs drafts",
  "license": "MIT",
  "contact": {
    "sources": "https://github.com/${repo}"
  },
  "environment": "*",
  "entrypoints": {
    "main": [
      "io.github.fablabsmc.blueprint.Blueprint"
    ]
  },
  "mixins": [
    "mixins.${id}.json"
  ],
  "depends": {
    "fabricloader": ">=0.4.0"
  },
  "custom": {
    "modmenu:api": true
  }
}
